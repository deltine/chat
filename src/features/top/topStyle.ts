import { makeStyles } from "@material-ui/core";

export const topStyles = makeStyles((theme) => ({
  tittle: { textAlign: "center", fontSize: 42, margin: "100px" },
  submit: { margin: theme.spacing(3, 0, 2) },
}));
